@using Microsoft.AspNetCore.Identity
@model KingPandaMedia.Models.ViewModels.LoginViewModel
@*@inject SignInManager<IdentityUser> SignInManager
    @inject UserManager<IdentityUser> UserManager*@
<li class="nav-item dropdown" style="text-transform: uppercase">
    <a class="nav-link dropdown-toggle" role="button" data-toggle="dropdown" id="navbardropdownmenulink" asp-controller="Login" asp-action="Login">Login</a>
    <div class="col-md-12 dropdown-menu">
        <a class="dropdown-item" asp-controller="Login" asp-action="Login">
            Login
            <form method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Email"></label>
                    <input asp-for="Email" class="form-control" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Password"></label>
                    <input asp-for="Password" class="form-control" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <div class="custom-checkbox">
                        <label asp-for="RememberLogin">
                            <input asp-for="RememberLogin" />@Html.DisplayNameFor(m => m.RememberLogin)
                        </label>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
            </form>
        </a>
    </div>
</li>
@*<ul class="navbar-nav">
        @if (SignInManager.IsSignedIn(User))
        {
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
            </li>
            <li class="nav-item">
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                </form>
            </li>
        }
        else
        {
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-controller="Users" asp-action="Create">Register</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-controller="Users" asp-action="Index">Login</a>
            </li>
        }
    </ul>*@
